apiVersion: apps/v1
kind: Deployment
metadata:
  name: voting-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: voting-postgres
  template:
    metadata:
      labels:
        app: voting-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: vote-dev-secrets
                  key: postgres_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: vote-dev-secrets
                  key: postgres_password
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: vote-dev-secrets
                  key: postgres_db
          ports:
            - containerPort: 5432
