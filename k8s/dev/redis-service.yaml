apiVersion: v1
kind: Service
metadata:
  name: voting-redis
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: voting-redis
  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: 6379
